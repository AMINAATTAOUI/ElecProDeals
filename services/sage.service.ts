/**
 * SageService — Couche d'abstraction vers Sage 100 Cloud
 *
 * RÈGLE ABSOLUE : Aucun code applicatif n'appelle directement les endpoints Sage.
 * Tout passe obligatoirement par ce service.
 *
 * Phase POC : retourne des données mockées avec les structures JSON exactes attendues.
 * Phase Production : remplacer l'implémentation interne uniquement, sans toucher aux appelants.
 */
export {};
